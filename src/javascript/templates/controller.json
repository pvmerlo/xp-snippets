{
  "Enonic XP Controller": {
    "body": [
      "",
      "exports.${1|get,post,all|} = handleRequest;",
      "",
      "function handleRequest(request) {",
      "",
      "    if (request.mode === 'edit') {",
      "        // do something...",
      "    }",
      "",
      "    var name = request.params.name;",
      "    log.info('Name = %s', name);",
      "",
      "    return {",
      "        body: 'Hello ' + name,",
      "        contentType: 'text/plain'",
      "    };",
      "};"
    ],
    "description": "Handles a Controller Request",
    "prefix": [
      "Enonic XP Controller",
      "XP Controller",
      "Handles Controller Requests",
      "handleGet",
      "handlePost",
      "handleAll",
      "xpController"
    ]
  }
}
